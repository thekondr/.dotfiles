# -*- mode: snippet -*-
# name: make-functional-component
# key: mf
# --
// @flow

import React from 'react'
import injectSheet from 'react-jss'

const styles = {
  root: {
    width: 100,
    height: 100,
    backgroundColor: 'red',
  },
}

type Props = {
  classes: any,
}

export function ${1:`(file-name-base)`}({ classes }: Props) {
  return (
    <div className={classes.root}>$0</div>
  )
}

export default injectSheet(styles)($1)