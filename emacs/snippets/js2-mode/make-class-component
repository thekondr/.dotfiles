# -*- mode: snippet -*-
# name: make-class-component
# key: mc
# --
// @flow

import React, { Component } from 'react'
import injectSheet from 'react-jss'

const styles = {
  root: {
    width: 100,
    height: 100,
    backgroundColor: 'red',
  },
}

type Props = {
  classes: any,
}

type State = {

}

export class ${1:`(file-name-base)`} extends Component<Props, State> {
  state: State = {

  }

  render() {
    const { classes } = this.props

    return (
      <div className={classes.root}>$0</div>
    )
  }
}

export default injectSheet(styles)($1)