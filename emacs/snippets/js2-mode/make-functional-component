# -*- mode: snippet -*-
# name: make-functional-component
# key: mf
# --
// @flow

import React from 'react'
import { withStyles } from '@material-ui/core/styles'

const styles = {
  root: {
    width: 100,
    height: 100,
    backgroundColor: 'red',
  },
}

type Props = {
  classes: any,
}

const ${1:`(file-name-base)`} = withStyles(styles)(({
  classes,
}: Props) => {
  return (
    <div className={classes.root}>$0</div>
  )
})

export default $1